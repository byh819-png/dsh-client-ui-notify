/**
 * The plugin-bundled ringtone: a two-tone chime synthesized at build time
 * (22050 Hz mono 16-bit PCM WAV), embedded as a base64 data URI so the client
 * bundle stays a single self-contained artifact — no extra asset route.
 */
/** Data URI the browser plays for the built-in notification ringtone. */
export declare const BUILTIN_RINGTONE_DATA_URI: string;
//# sourceMappingURL=builtin-ringtone.d.ts.map